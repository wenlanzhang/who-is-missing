# Script dependencies
geopandas>=0.14
mercantile>=1.2  # quadkey-to-geometry for Meta_base scripts
rasterio>=1.4
rasterstats>=0.19
numpy>=1.20
pandas>=1.0
matplotlib>=3.0
scipy>=1.7
statsmodels>=0.13  # for regression with controls, VIF
libpysal>=4.0
esda>=2.0  # Moran's I (spatial autocorrelation)
spreg>=1.2  # spatial regression (SLM, SEM)
contextily>=1.4  # basemap tiles for residual maps (Sentinel-2, Esri, OSM)
xyzservices>=2023.0  # tile providers for contextily
osmnx>=1.9  # optional; OSM city boundaries (--clip-source osm). Nominatim is used if missing.
